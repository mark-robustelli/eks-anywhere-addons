apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fusion-external-secret
  namespace: fusionauth
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: eksa-secret-store #The secret store name we have just created.
    kind: ClusterSecretStore
  target:
    name: FA-EKS-A-Secrets # Secret name in k8s
  data:
  - secretKey: FA_ROOT_DB_NAME # which key it's going to be stored
    remoteRef:
      key: FA-root-db-username # Our secret-name goes here